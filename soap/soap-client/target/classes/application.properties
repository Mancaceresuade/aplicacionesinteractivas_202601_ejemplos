server.port=8081

# URL del servidor SOAP.
# En Docker usa el nombre del servicio como hostname (soap-server).
# Para correr localmente fuera de Docker, cambiar a: http://localhost:8080/ws
soap.server.url=${SOAP_SERVER_URL:http://localhost:8080/ws}
